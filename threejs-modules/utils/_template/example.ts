import { templateUtil } from './index'

// Minimal example — copy và đổi templateUtil thành tên export thật
export function runExample() {
  const result = templateUtil(undefined)
  console.log('[example]', result)
  return result
}
