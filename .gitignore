# Project apps — each has its own git repo, cloned standalone
00-Threejs/
01-Doreamon/

# threejs-modules/ — tracked directly by Private-threejs (no separate repo)

# Ecosystem-level — managed by Web-3D root repo (Web-3D-Ecosystem)
.gemini/
SYNC.md
GEMINI.md
ECOSYSTEM.md

# .claude/ tracked by Private-threejs — engine skills live here

# validate.js cache — local only, không share
.validate-cache.json

# Claude Code local settings — machine-specific paths
.claude/settings.local.json
